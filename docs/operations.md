# 運用ドキュメント

## 👉 まずはこちらを見てください

**実装コントロールパネル（実行ガイド）**
https://claude.ai/code/artifact/d6bac243-6942-4709-870e-11de8d0eee48

稼働状況・Apps Scriptの手順・ドメイン判断・ネクストアクションを1枚にまとめています。
（ソース: `docs/control-panel.html`）

---

以下は技術的な補足情報です。日常の作業では上記のパネルだけ見れば十分です。

## Apps Script 関数リファレンス

### 00-diagnose.gs — 調査・緊急停止

| 関数 | 書き込み | 用途 |
|------|---------|------|
| `shindan()` | なし | 全タブ名と列構成を表示 |
| `torigaIchiran()` | なし | 設定中のトリガー一覧 |
| `zenTriggerSakujo()` | — | 全トリガー削除（緊急停止） |

### 01-media-ingest.gs — 応募メール取込

対象: 求人媒体スプシ `15B9X9aE0d9JgY4B0o_ainBYC2gHMfFbT2_sUL7FkkjI`

| 関数 | 書き込み | 用途 |
|------|---------|------|
| `chosa_ouboMail()` | なし | 応募メールのサンプルと抽出テスト |
| `torikomi_TEST()` | なし | 取込件数のみ表示 |
| `torikomi_HONBAN()` | あり | 実際に追記 |
| `triggerSettei()` | — | 15分おき自動実行を有効化 |

対応媒体: Indeed / 求人ボックス / engage / ジョブオレ
重複判定: 電話番号の下10桁
二重取込防止: Gmailラベル「取込済み」

### 02-nurturing.gs — ナーチャリング

対象: 広告面談スプシ `1wEKLJ-J-cPiso-9TfxkW7oLcOFbcPTIVvtrSznfUtLQ`

| 関数 | 送信 | 用途 |
|------|------|------|
| `kakunin_TEST()` | なし | 温度感分布・Hot一覧を表示 |
| `hotLeadTsuchi()` | あり | Hot判定をCAへ通知 |
| `menndanRemind()` | あり | 面談リマインダー |
| `nichijiKPI()` | あり | 日次KPIレポート |
| `nurtureTriggerSettei()` | — | 自動実行を有効化（8/10/15/18時） |

### 温度感スコア配分（最大100点、Hot = 55点以上）

| 項目 | 配点 |
|------|------|
| 希望就職時期（今すぐ35 / 1〜2ヶ月26 / 3〜4ヶ月14 / 5ヶ月6） | 35 |
| フェーズ（選考中30 / 求人提案22 / 面談実施14 / 面談設定8） | 30 |
| 承認/否認が「〇」 | 15 |
| 面談実施が「〇」 | 10 |
| 正社員経験（3年以上10 / その他経験あり5） | 10 |

「その他離脱」「求人紹介不可」は全て集計対象外。
閾値は `NURTURE_CONFIG.HOT_THRESHOLD` で変更可能。

## スクリプトプロパティ

プロジェクトの設定 → スクリプト プロパティ

| キー | 用途 | 必須 |
|------|------|------|
| `SLACK_WEBHOOK_URL` | Slack通知先 | 任意 |
| `EMAIL_MATSUMOTO` | 松本さん宛メール | 任意 |
| `EMAIL_ARAKI` | 荒木さん宛メール | 任意 |
| `EMAIL_GOSHI` | 合志さん宛メール | 任意 |
| `HUBSPOT_API_KEY` | HubSpot連携（将来） | 任意 |

未設定の項目は通知がスキップされ、実行ログにのみ出力されます。

## サイト調査結果（2026-08-19 実測）

| 項目 | 結果 |
|------|------|
| corekara.info | 稼働中 / Firebase Hosting (199.36.158.100) |
| HTML内の本文 | `<div id="root"></div>` のみ |
| JS無効時のテキスト量 | 54文字 |
| `<html lang>` | `en`（日本語サイトだが英語指定＝要修正） |
| careerjapan.co | Aレコードなし（Web未設定・メールは稼働） |

**影響:** AI検索クローラーの多くはJavaScriptを実行しないため、現状のLLMO施策は土台が機能しません。SSR化が前提条件です。

## セキュリティ

求人媒体スプシの1枚目タブに、各媒体のログインIDとパスワードが平文で記載されています。
パスワード管理ツールへの移行を推奨します。コード・リポジトリには一切記録していません。
